# ponytail: H2 keeps `mvn test` runnable without Docker; nothing here is Postgres-specific yet.
spring.datasource.url=jdbc:h2:mem:swendoc;DB_CLOSE_DELAY=-1
spring.jpa.hibernate.ddl-auto=create-drop

minio.url=http://localhost:9000
minio.access-key=test
minio.secret-key=test
minio.bucket=documents
minio.init-bucket=false
